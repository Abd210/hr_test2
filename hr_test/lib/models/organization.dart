class Organization {
  final int id;
  String name;
  String description;
  DateTime createdAt;

  Organization({
    required this.id,
    required this.name,
    required this.description,
    required this.createdAt,
  });
}
